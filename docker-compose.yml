services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: legacy_modernization
      POSTGRES_USER: legacy
      POSTGRES_PASSWORD: legacy
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
